# .vscode/preview.yml
autoOpen: false # 打开工作空间时是否自动开启所有应用的预览
apps:
  # 以 hello-world.js 为例
  - port: 3000 # 应用的端口
    run:  bash start.sh > log.txt 2>&1 # 应用的启动命令
    root: ./ # 应用的启动目录
    name: my-app # 应用名称
    description: 我的第一个 App。 # 应用描述
    autoOpen: false # 打开工作空间时是否自动开启预览（优先级高于根级 autoOpen）
  # - run: nohup bin/ngrok http --url=capable-sunbird-publicly.ngrok-free.app 80 ngrok.log 2>&1 &
  #   name: ngrok
  #   root: ./
  #   autoOpen: true
    